cmake_minimum_required(VERSION 3.6)
project(StabilizerCPP)

set(CMAKE_CXX_STANDARD 11)

#if(WIN32)
#    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DNOMINMAX /W2")
#    set(CMAKE_CXX_WARNING_LEVEL 2)
#    if(BUILD_SHARED AND WITHIN_SUPERBUILD)
#        set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DBOOST_LIB_PREFIX=\"\"")
#    endif()
#endif(WIN32)
MESSAGE(${CMAKE_SOURCE_DIR})
set(NO_BOOST_CMAKE TRUE)
find_package(BOOST REQUIRED)
if(Boost_FOUND)
    include_directories(${Boost_INCLUDE_DIRS})
#else()
#    set(BOOST_ROOT ${CMAKE_SOURCE_DIR}../../Documents/Installed/boost_1_63_0)
#    find_package(BOOST)
endif()
include_directories( ${CMAKE_SOURCE_DIR}/src/lib )
set(SOURCE_FILES src/main.cpp src/StabilizerGroup.cpp src/SymplecticPauli.cpp)
add_executable(StabilizerCPP ${SOURCE_FILES})
target_link_libraries(StabilizerCPP ${Boost_LIBRARIES})
